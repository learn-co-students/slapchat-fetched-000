# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'

target 'slapChat' do

end

target 'slapChatTests' do
  pod 'Specta', '~> 1.0'
  pod 'Expecta',     '~> 1.0'                                                 
  pod 'KIF', '~> 3.3'   
end
